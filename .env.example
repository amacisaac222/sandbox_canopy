# Agent Sandbox Configuration

# Database URL (SQLite for dev, Postgres for prod)
CP_DB_URL=sqlite+aiosqlite:///./sandbox.db

# Secret for signing policy bundles (CHANGE IN PRODUCTION!)
CP_TENANT_SECRET=devsecret_change_me

# Control plane binding
CP_BIND=0.0.0.0
CP_PORT=8080